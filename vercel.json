{
  "version": 2,
  "name": "todo-list",
  "builds": [
    {
      "src": "apps/todo",
      "use": "@vercel/static-build",
      "config": { "distDir": "dist/todo" }
    },
    {
      "src": "apps/api",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "/apps/api/$1" },
    { "src": "/(.*)", "dest": "/apps/todo/$1" }
  ]
}
