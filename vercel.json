{
  "version": 2,
  "name": "todo-list",
  "builds": [
    {
      "src": "apps/todo",
      "use": "@vercel/static-build",
      "config": { "distDir": "dist" }
    },
    {
      "src": "apps/api",
      "use": "@vercel/node",
      "config": {
        "maxDuration": 60,
        "memory": 1024,
        "builds": [{ "src": "", "use": "@vercel/static" }]
      }
    }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "/apps/api/$1" },
    { "src": "/(.*)", "dest": "/apps/todo/$1" }
  ]
}
